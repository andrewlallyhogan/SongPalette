import '@testing-library/jest-dom';
import React from 'react';
import { render, screen, cleanup } from '@testing-library/react';
import UserInformation from '../../client/src/UserInformation';

xdescribe('1 should equal 1', () => {
  expect(1).toEqual(1);
})
